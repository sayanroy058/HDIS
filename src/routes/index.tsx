import { createFileRoute, Link } from "@tanstack/react-router";
import { Activity, ArrowRight, BrainCircuit, ShieldCheck, Sparkles } from "lucide-react";

// No head() here: the home route inherits title/description/og/twitter from
// __root.tsx, and ships no og:image so serve-time hosting can inject the
// project's social preview (explicit og:image or latest screenshot).
export const Route = createFileRoute("/")({
  component: Index,
});

// IMPORTANT: Replace this placeholder. See ./README.md for routing conventions.
function Index() {
  const features = [[BrainCircuit, "Connect the signal", "Turn scattered observations into one shared patient story."], [ShieldCheck, "Support decisions", "Make evidence and uncertainty visible at the moment of care."], [Activity, "Stay ahead", "Spot deterioration early with live trends and explainable risk."]] as const;
  return <main className="min-h-screen bg-background text-foreground"><div className="mx-auto max-w-7xl px-6 py-6 lg:px-10"><header className="flex items-center justify-between"><Link to="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-primary text-primary-foreground"><Activity className="size-5" /></span><b className="text-xl tracking-tight">HDIS</b></Link><Link to="/dashboard" className="rounded-xl bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground">Open workspace <ArrowRight className="ml-1 inline size-4" /></Link></header><section className="grid items-center gap-12 pb-20 pt-24 lg:grid-cols-[1fr_0.9fr]"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-primary/20 bg-accent px-3 py-1.5 text-xs font-bold text-accent-foreground"><Sparkles className="size-3.5" /> Built for the bedside</div><h1 className="max-w-2xl text-5xl font-extrabold leading-[1.05] tracking-[-0.04em] sm:text-7xl">See the whole patient.<br /><span className="text-primary">Act with confidence.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted-foreground">HDIS brings live physiology, clinical evidence, and treatment simulation into one calm workspace for critical care teams.</p><Link to="/dashboard" className="mt-8 inline-block rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground shadow-xl shadow-primary/20">Explore the prototype <ArrowRight className="ml-2 inline size-4" /></Link></div><div className="rounded-[2rem] border border-border bg-card p-4 shadow-2xl shadow-primary/10"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-bold">ICU command view</p><p className="text-[10px] text-muted-foreground">Patient monitoring · Bed 07</p></div><span className="rounded-full bg-critical-soft px-2 py-1 text-[10px] font-bold text-critical">HIGH RISK</span></div><div className="grid grid-cols-2 gap-3">{[["Heart rate", "124", "bpm"], ["MAP", "62", "mmHg"], ["SpO₂", "91", "%"], ["Lactate", "4.2", "mmol/L"]].map(([label, value, unit]) => <div key={label} className="rounded-xl bg-muted p-3"><p className="text-[10px] text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-extrabold text-critical">{value}<small className="ml-1 text-[10px] font-medium text-muted-foreground">{unit}</small></p><div className="mt-3 h-1 rounded-full bg-critical/60" /></div>)}</div><div className="mt-3 rounded-xl border border-primary/10 bg-accent p-3"><p className="text-xs font-bold text-accent-foreground">Recommended next step</p><p className="mt-1 text-[11px] text-muted-foreground">Start broad-spectrum antibiotics + fluids, then reassess MAP.</p></div></div></section><section className="grid gap-4 border-t border-border py-16 md:grid-cols-3">{features.map(([Icon, title, body]) => <div key={title} className="rounded-2xl border border-border bg-card p-6"><Icon className="size-5 text-primary" /><h2 className="mt-5 text-lg font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{body}</p></div>)}</section></div></main>;
}
