import { createFileRoute } from "@tanstack/react-router";
import { Check, Clock3, Plus, Send } from "lucide-react";
import { Panel, PanelHead, Pill } from "@/components/hdis/primitives";

export const Route = createFileRoute("/dashboard/orders")({ component: OrdersPage });
const orders = [{ name: "Blood cultures × 2", detail: "Before antibiotics · STAT", status: "Pending" }, { name: "Piperacillin / tazobactam", detail: "4.5 g IV · Once now", status: "Recommended" }, { name: "Balanced crystalloid", detail: "500 mL IV bolus · Reassess MAP", status: "Recommended" }, { name: "Arterial blood gas", detail: "Room air · STAT", status: "Pending" }];
function OrdersPage() { return <div className="grid gap-4 xl:grid-cols-[1.2fr_0.8fr]"><Panel><PanelHead title="Orders & Care Plan" hint="Review, stage, and document the next steps" action={<button className="rounded-lg bg-primary px-3 py-2 text-xs font-bold text-primary-foreground"><Plus className="mr-1 inline size-3" /> Add order</button>} /><div className="space-y-2">{orders.map((o, i) => <div key={o.name} className="flex items-center gap-3 rounded-xl border border-border p-4"><span className={`grid size-8 place-items-center rounded-full ${i === 1 || i === 2 ? "bg-good-soft text-good" : "bg-muted text-muted-foreground"}`}>{i === 1 || i === 2 ? <Check className="size-4" /> : <Clock3 className="size-4" />}</span><div className="flex-1"><p className="text-sm font-bold">{o.name}</p><p className="text-xs text-muted-foreground">{o.detail}</p></div><Pill tone={o.status === "Recommended" ? "good" : "warn"}>{o.status}</Pill></div>)}</div></Panel><Panel><PanelHead title="Order basket" hint="Ready to send to the care team" /><div className="rounded-xl bg-accent p-4"><p className="text-sm font-bold">4 orders staged</p><p className="mt-1 text-xs text-muted-foreground">All values are mock data for this prototype.</p></div><button className="mt-4 w-full rounded-xl bg-primary py-3 text-sm font-bold text-primary-foreground"><Send className="mr-2 inline size-4" /> Send for review</button></Panel></div>; }
