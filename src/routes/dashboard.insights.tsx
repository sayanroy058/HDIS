import { createFileRoute } from "@tanstack/react-router";
import { ArrowUpRight, Lightbulb, ThumbsUp } from "lucide-react";
import { Panel, PanelHead, Pill } from "@/components/hdis/primitives";
export const Route = createFileRoute("/dashboard/insights")({ component: InsightsPage });
const insights = [{ title: "MAP decline precedes lactate rise by ~45 min", body: "Across similar ICU episodes, early perfusion changes were the clearest leading signal.", tone: "critical" as const }, { title: "Fluid response is likely limited", body: "Current hemodynamic pattern resembles patients who benefited from vasopressor support.", tone: "warn" as const }, { title: "Central line is a plausible source", body: "Fever, timing, and current cultures make source control worth prioritising.", tone: "info" as const }];
function InsightsPage() { return <div className="grid gap-4 xl:grid-cols-[1fr_0.7fr]"><Panel><PanelHead title="Learned Insights" hint="Explainable patterns from the HDIS clinical model" /><div className="space-y-3">{insights.map(i => <div key={i.title} className="rounded-xl border border-border p-4"><div className="flex items-start gap-3"><span className="grid size-9 place-items-center rounded-xl bg-accent text-accent-foreground"><Lightbulb className="size-4" /></span><div className="flex-1"><div className="flex justify-between gap-3"><p className="text-sm font-bold">{i.title}</p><Pill tone={i.tone}>{i.tone}</Pill></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{i.body}</p><button className="mt-3 text-xs font-bold text-primary">View supporting evidence <ArrowUpRight className="ml-1 inline size-3" /></button></div></div></div>)}</div></Panel><Panel><PanelHead title="Model feedback" hint="Help HDIS learn what is useful" /><div className="rounded-xl bg-good-soft p-4"><ThumbsUp className="size-5 text-good" /><p className="mt-3 text-sm font-bold">Your feedback matters</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Insights are suggestions, never instructions. Rate them to improve this mock experience.</p></div><button className="mt-4 w-full rounded-xl border border-border py-3 text-sm font-bold">Review recent feedback</button></Panel></div>; }
