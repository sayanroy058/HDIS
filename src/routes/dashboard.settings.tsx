import { createFileRoute } from "@tanstack/react-router";
import { Bell, Check, ChevronRight, SlidersHorizontal } from "lucide-react";
import { Panel, PanelHead } from "@/components/hdis/primitives";
export const Route = createFileRoute("/dashboard/settings")({ component: SettingsPage });
function SettingsPage() { return <div className="grid gap-4 xl:grid-cols-[0.8fr_1.2fr]"><Panel><PanelHead title="Settings" hint="Configure your HDIS workspace" /><div className="space-y-1">{["Workspace preferences", "Alert thresholds", "Notification channels", "Team & permissions", "Data & privacy"].map((x, i) => <button key={x} className={`flex w-full items-center gap-3 rounded-xl p-3 text-left text-sm font-semibold ${i === 0 ? "bg-accent text-accent-foreground" : "hover:bg-muted"}`}><SlidersHorizontal className="size-4" />{x}<ChevronRight className="ml-auto size-4" /></button>)}</div></Panel><Panel><PanelHead title="Workspace preferences" hint="Personalise your clinical view" /><div className="space-y-5">{[["Compact clinical cards", "Show more data in each panel"], ["Trend change alerts", "Notify me when a vital changes quickly"], ["Sound for critical alerts", "Use an audible cue for high risk"]].map(([title, desc], i) => <div key={title} className="flex items-center justify-between gap-4"><div><p className="text-sm font-bold">{title}</p><p className="text-xs text-muted-foreground">{desc}</p></div><span className={`grid size-6 place-items-center rounded-md ${i < 2 ? "bg-primary text-primary-foreground" : "border border-border text-transparent"}`}><Check className="size-4" /></span></div>)}<div className="border-t border-border pt-5"><p className="flex items-center gap-2 text-sm font-bold"><Bell className="size-4 text-primary" /> Alert digest</p><p className="mt-1 text-xs text-muted-foreground">Critical alerts are always shown immediately.</p></div></div></Panel></div>; }
